<% content_for :title, "Leaderboard — #{@league.name}" %>

<div class="lb-page" data-current-user-id="<%= @current_entry_id %>"
     data-end-time="<%= @league.end_date&.to_time&.end_of_day&.to_i&.*(1000) %>">

  <%# ── Leaderboard Header ── %>
  <div class="lb-comp-bar">
    <div class="lb-comp-selector">
      <span class="lb-comp-label">LEAGUE</span>
      <span class="lb-comp-current"><%= @league.name.upcase %></span>
    </div>
    <div class="lb-comp-actions">
      <%= link_to "ARCHIVE", leaderboard_path, class: "terminal-btn" %>
      <button class="terminal-btn" type="button" onclick="document.getElementById('lb-prizes-modal').style.display='flex'">PRIZES</button>
      <%= link_to "RULES", league_path(@league), class: "terminal-btn" %>
      <%= link_to "BACK", league_path(@league), class: "terminal-btn" %>
    </div>
  </div>

  <%# ── Status Strip ── %>
  <div class="lb-status-strip">
    <div class="lb-status-item">
      <span class="lb-status-label">STATUS</span>
      <% if @league.end_date && @league.end_date < Date.current %>
        <span class="lb-status-value lb-status-finished">FINISHED</span>
      <% else %>
        <span class="lb-status-value lb-status-live">LIVE</span>
      <% end %>
    </div>
    <div class="lb-status-item lb-status-timer">
      <span class="lb-status-label">TIME REMAINING</span>
      <% if @league.end_date && @league.end_date >= Date.current %>
        <span class="lb-status-value lb-countdown" id="lb-countdown">--D --:--:--</span>
      <% elsif @league.end_date %>
        <span class="lb-status-value lb-status-finished">ENDED</span>
      <% else %>
        <span class="lb-status-value">ONGOING</span>
      <% end %>
    </div>
    <div class="lb-status-item">
      <span class="lb-status-label"><%= @league.team_mode? ? "TEAMS" : "CONTESTANTS" %></span>
      <span class="lb-status-value"><%= @rankings.size %></span>
    </div>
    <div class="lb-status-item">
      <span class="lb-status-label"><%= @league.end_date && @league.end_date < Date.current ? "FINAL" : "CURRENT" %> LEADER</span>
      <span class="lb-status-value lb-status-leader"><%= @rankings.first&.dig(@league.team_mode? ? :team_name : :name) || "—" %></span>
    </div>
    <div class="lb-status-item lb-status-update">
      <span class="lb-status-label">UPDATES EVERY</span>
      <span class="lb-status-value"><%= @update_interval_minutes %> MIN</span>
      <span class="lb-status-sub">Last: <%= @last_updated_at.strftime("%H:%M:%S") %></span>
    </div>
  </div>

  <%# ── Main Content: Table + Detail Panel ── %>
  <div class="lb-content">

    <%# ── Leaderboard Table (Left) ── %>
    <div class="lb-table-wrap">
      <div class="terminal-window lb-table-window">
        <div class="terminal-window-title">RANKINGS</div>
        <div class="lb-table-scroll" id="lb-table-scroll">
          <table class="terminal-table lb-table">
            <thead>
              <tr>
                <th class="num lb-col-rank">RANK</th>
                <th class="lb-col-name"><%= @league.team_mode? ? "TEAM" : "NAME" %></th>
                <th class="num lb-col-return">RETURN %</th>
                <th class="num lb-col-value">PORTFOLIO VALUE</th>
                <th class="lb-col-trend">TREND</th>
              </tr>
            </thead>
            <tbody id="lb-table-body">
              <% @rankings.each do |row| %>
                <% entry_id = @league.team_mode? ? row[:team_id] : row[:user_id] %>
                <% entry_name = @league.team_mode? ? row[:team_name] : row[:name] %>
                <% entry_return = @league.team_mode? ? row[:avg_return_pct] : row[:total_return_pct] %>
                <% entry_value = @league.team_mode? ? row[:avg_portfolio_value] : row[:portfolio_value] %>
                <tr class="lb-row <%= 'lb-row-top3' if row[:rank] <= 3 %> <%= 'lb-row-self' if entry_id == @current_entry_id %>"
                    data-user-id="<%= entry_id %>"
                    onclick="window.lbSelectUser(<%= entry_id %>)">
                  <td class="num lb-rank-cell">
                    <% if row[:rank] <= 3 %>
                      <span class="lb-medal lb-medal-<%= row[:rank] %>">#<%= row[:rank] %></span>
                    <% else %>
                      #<%= row[:rank] %>
                    <% end %>
                    <% if row[:rank_delta].present? && row[:rank_delta] != 0 %>
                      <span class="lb-rank-delta <%= row[:rank_delta] < 0 ? 'lb-rank-delta--up' : 'lb-rank-delta--down' %>">
                        <%= row[:rank_delta] < 0 ? "▲#{row[:rank_delta].abs}" : "▼#{row[:rank_delta]}" %>
                      </span>
                    <% elsif row[:rank_delta] == 0 %>
                      <span class="lb-rank-delta lb-rank-delta--flat">—</span>
                    <% end %>
                  </td>
                  <td class="lb-name-cell">
                    <%= entry_name %>
                    <% if @league.team_mode? %>
                      <% if row[:eligible_for_final_ranking] %>
                        <span class="terminal-badge-active" style="margin-left: 0.4rem;">ELIGIBLE</span>
                      <% else %>
                        <span class="terminal-badge-inactive" style="margin-left: 0.4rem;">NOT ELIGIBLE</span>
                      <% end %>
                    <% end %>
                  </td>
                  <td class="num <%= entry_return >= 0 ? 'terminal-text-up' : 'terminal-text-down' %>">
                    <%= sprintf("%+.2f%%", entry_return) %>
                  </td>
                  <td class="num"><%= number_to_currency(entry_value, precision: 2) %></td>
                  <td class="lb-trend-cell">
                    <svg class="lb-sparkline" viewBox="0 0 70 20" preserveAspectRatio="none">
                      <% vals = row[:trend] %>
                      <% mn = vals.min.to_f; mx = vals.max.to_f; rng = mx - mn; rng = 1 if rng < 1 %>
                      <% pts = vals.each_with_index.map { |v, j| x = (j.to_f / (vals.size - 1) * 68 + 1).round(1); y = (18 - (v - mn) / rng * 16).round(1); "#{x},#{y}" }.join(" ") %>
                      <polyline fill="none"
                                stroke="<%= entry_return >= 0 ? '#30d158' : '#ff453a' %>"
                                stroke-width="1.5"
                                points="<%= pts %>" />
                    </svg>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>

        <%# ── Sticky Self Row ── %>
        <% self_row = if @league.team_mode?
          @rankings.find { |r| r[:team_id] == @current_entry_id }
        else
          @rankings.find { |r| r[:user_id] == @current_entry_id }
        end %>
        <% if self_row && @current_entry_id.to_i > 0 %>
          <div class="lb-sticky-self" id="lb-sticky-self">
            <div class="lb-sticky-self-inner">
              <span class="lb-sticky-label"><%= @league.team_mode? ? "YOUR TEAM" : "YOUR POSITION" %></span>
              <span class="lb-sticky-rank">#<%= self_row[:rank] %></span>
              <span class="lb-sticky-name"><%= @league.team_mode? ? self_row[:team_name] : self_row[:name] %></span>
              <% sticky_return = @league.team_mode? ? self_row[:avg_return_pct] : self_row[:total_return_pct] %>
              <span class="lb-sticky-return <%= sticky_return >= 0 ? 'terminal-text-up' : 'terminal-text-down' %>">
                <%= sprintf("%+.2f%%", sticky_return) %>
              </span>
              <% sticky_value = @league.team_mode? ? self_row[:avg_portfolio_value] : self_row[:portfolio_value] %>
              <span class="lb-sticky-value"><%= number_to_currency(sticky_value, precision: 2) %></span>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <%# ── Detail Panel (Right) ── %>
    <div class="lb-detail-panel" id="lb-detail-panel">
      <div class="lb-detail-placeholder" id="lb-detail-placeholder">
        <span class="lb-detail-icon">◉</span>
        <p>Select a participant to view details</p>
      </div>

      <% @rankings.each do |row| %>
        <% entry_id = @league.team_mode? ? row[:team_id] : row[:user_id] %>
        <% entry_name = @league.team_mode? ? row[:team_name] : row[:name] %>
        <% entry_return = @league.team_mode? ? row[:avg_return_pct] : row[:total_return_pct] %>
        <% entry_value = @league.team_mode? ? row[:avg_portfolio_value] : row[:portfolio_value] %>
        <div class="lb-detail-card" id="lb-detail-<%= entry_id %>" style="display:none">
          <div class="lb-detail-header">
            <div class="lb-detail-rank-badge">#<%= row[:rank] %></div>
            <div>
              <div class="lb-detail-name"><%= entry_name %></div>
              <% if @league.team_mode? %>
                <div class="lb-detail-sub">Members: <%= row[:member_count] %></div>
              <% else %>
                <div class="lb-detail-sub">Highest rank: #<%= row[:highest_rank] %></div>
              <% end %>
            </div>
          </div>

          <div class="lb-detail-stats">
            <div class="lb-detail-stat">
              <span class="lb-detail-stat-label">STARTING BALANCE</span>
              <span class="lb-detail-stat-value"><%= number_to_currency(@league.starting_capital.to_f, precision: 0) %></span>
            </div>
            <div class="lb-detail-stat">
              <span class="lb-detail-stat-label">PORTFOLIO VALUE</span>
              <span class="lb-detail-stat-value"><%= number_to_currency(entry_value, precision: 0) %></span>
            </div>
            <div class="lb-detail-stat">
              <span class="lb-detail-stat-label">TOTAL RETURN</span>
              <span class="lb-detail-stat-value <%= entry_return >= 0 ? 'terminal-text-up' : 'terminal-text-down' %>">
                <%= sprintf("%+.2f%%", entry_return) %>
              </span>
            </div>
            <div class="lb-detail-stat">
              <span class="lb-detail-stat-label">GAIN / LOSS</span>
              <% gain = entry_value - @league.starting_capital.to_f %>
              <span class="lb-detail-stat-value <%= gain >= 0 ? 'terminal-text-up' : 'terminal-text-down' %>">
                <%= number_to_currency(gain, precision: 0) %>
              </span>
            </div>
          </div>

          <%# ── Equity Curve Chart ── %>
          <div class="lb-detail-chart-section">
            <div class="lb-detail-chart-title">BALANCE HISTORY</div>
            <div class="lb-detail-chart-container">
              <%# Y-axis labels %>
              <div class="lb-chart-yaxis">
                <% vals = row[:trend]; mn = vals.min.to_f; mx = vals.max.to_f; rng = mx - mn; rng = 1 if rng < 1 %>
                <span><%= number_to_currency(mx, precision: 0) %></span>
                <span><%= number_to_currency(mn + rng * 0.75, precision: 0) %></span>
                <span><%= number_to_currency(mn + rng * 0.5, precision: 0) %></span>
                <span><%= number_to_currency(mn + rng * 0.25, precision: 0) %></span>
                <span><%= number_to_currency(mn, precision: 0) %></span>
              </div>
              <div class="lb-chart-main">
                <div class="lb-chart-svg-wrap">
                  <svg class="lb-detail-chart-svg" viewBox="0 0 280 100" preserveAspectRatio="none">
                    <% step_x = 280.0 / (vals.size - 1) %>
                    <% area_pts = vals.each_with_index.map { |v, j| "#{(j * step_x).round(1)},#{(95 - (v - mn) / rng * 85).round(1)}" } %>
                    <polygon fill="<%= entry_return >= 0 ? 'rgba(48,209,88,0.12)' : 'rgba(255,69,58,0.12)' %>"
                             points="0,95 <%= area_pts.join(' ') %> 280,95" />
                    <polyline fill="none"
                              stroke="<%= entry_return >= 0 ? '#30d158' : '#ff453a' %>"
                              stroke-width="2"
                              points="<%= area_pts.join(' ') %>" />
                    <% vals.each_with_index do |v, j| %>
                      <% x = (j * step_x).round(1); y = (95 - (v - mn) / rng * 85).round(1) %>
                      <circle cx="<%= x %>" cy="<%= y %>" r="3"
                              fill="<%= entry_return >= 0 ? '#30d158' : '#ff453a' %>"
                              stroke="#1c1c1e" stroke-width="1"/>
                    <% end %>
                  </svg>
                </div>
                <%# X-axis labels %>
                <div class="lb-chart-xaxis">
                  <span>Day 1</span>
                  <span>Day 4</span>
                  <span>Day 7</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>

  </div>

  <div class="terminal-disclaimer-bar">
    Virtual trading only · Rankings update every <%= @update_interval_minutes %> minutes · No real funds involved
  </div>

  <%# ── Prizes Modal ── %>
  <div class="lb-modal-overlay" id="lb-prizes-modal" onclick="if(event.target===this) this.style.display='none'">
    <div class="lb-modal-card">
      <div class="lb-modal-header">
        <h2 class="lb-modal-title">PRIZES</h2>
        <button class="lb-modal-close" type="button" onclick="document.getElementById('lb-prizes-modal').style.display='none'">✕</button>
      </div>
      <div class="lb-modal-body">
        <% @prizes.each do |p| %>
          <div class="lb-prize-row">
            <span class="lb-prize-rank"><%= p[:rank] %></span>
            <span class="lb-prize-desc"><%= p[:prize] %></span>
          </div>
        <% end %>
        <% if @prizes.empty? %>
          <p class="lb-modal-empty">No prizes configured for this league.</p>
        <% end %>
      </div>
    </div>
  </div>
</div>

<%= javascript_tag nonce: true do %>
(function() {
  var activeUserId = null;
  var stickyEl = document.getElementById('lb-sticky-self');
  var scrollEl = document.getElementById('lb-table-scroll');
  var countdownEl = document.getElementById('lb-countdown');
  var pageEl = document.querySelector('.lb-page');

  if (countdownEl && pageEl.dataset.endTime) {
    var endTime = parseInt(pageEl.dataset.endTime, 10);
    function updateCountdown() {
      var now = Date.now();
      var diff = endTime - now;
      if (diff <= 0) {
        countdownEl.textContent = 'ENDED';
        countdownEl.classList.add('lb-status-finished');
        return;
      }
      var days = Math.floor(diff / 86400000);
      var hrs = Math.floor((diff % 86400000) / 3600000);
      var mins = Math.floor((diff % 3600000) / 60000);
      var secs = Math.floor((diff % 60000) / 1000);
      countdownEl.textContent = days + 'D ' +
        String(hrs).padStart(2,'0') + ':' +
        String(mins).padStart(2,'0') + ':' +
        String(secs).padStart(2,'0');
    }
    updateCountdown();
    setInterval(updateCountdown, 1000);
  }

  window.lbSelectUser = function(userId) {
    document.querySelectorAll('.lb-detail-card').forEach(function(c) { c.style.display = 'none'; });
    document.querySelectorAll('.lb-row').forEach(function(r) { r.classList.remove('lb-row-selected'); });

    var card = document.getElementById('lb-detail-' + userId);
    var row = document.querySelector('[data-user-id="' + userId + '"]');
    var placeholder = document.getElementById('lb-detail-placeholder');

    if (activeUserId === userId) {
      activeUserId = null;
      if (placeholder) placeholder.style.display = '';
      return;
    }

    activeUserId = userId;
    if (card) card.style.display = '';
    if (row) row.classList.add('lb-row-selected');
    if (placeholder) placeholder.style.display = 'none';
  };

  if (stickyEl && scrollEl) {
    var selfRow = document.querySelector('.lb-row-self');
    function checkStickyVisibility() {
      if (!selfRow) return;
      var scrollRect = scrollEl.getBoundingClientRect();
      var rowRect = selfRow.getBoundingClientRect();
      var isVisible = rowRect.top < scrollRect.bottom && rowRect.bottom > scrollRect.top;
      stickyEl.classList.toggle('lb-sticky-hidden', isVisible);
    }
    scrollEl.addEventListener('scroll', checkStickyVisibility);
    window.addEventListener('resize', checkStickyVisibility);
    checkStickyVisibility();
  }
})();
<% end %>
