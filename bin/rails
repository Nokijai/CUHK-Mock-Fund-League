#!/usr/bin/env ruby
# frozen_string_literal: true

# Rails binstub (so `bin/rails` runs the app commands, not `rails new`).
APP_PATH = File.expand_path("../config/application", __dir__)
require_relative "../config/boot"
require "rails/commands"
